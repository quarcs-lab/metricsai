{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "fp=$(cat | jq -r '.tool_input.file_path // empty'); if [[ \"$fp\" == */notebooks_quarto/*.qmd ]]; then bash \"$CLAUDE_PROJECT_DIR/scripts/sync_chapter.sh\" \"$fp\"; fi"
          }
        ]
      }
    ]
  }
}
