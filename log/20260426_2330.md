# 2026-04-26 23:30 — Post-Migration Audit: Fix Pyfixest Issues

## Work Done

Comprehensive audit of the pyfixest migration (120 files changed in prior session) found and fixed **18 issues** across 6 .qmd notebooks.

### CRITICAL Fixes (4) — Would cause runtime errors

| File | Line | Issue | Fix |
|------|------|-------|-----|
| ch15 | 1275 | `ols_mix.params[v]` — undefined variable | → `fit_mix.coef()[v]` |
| ch16 | 848 | `model_bivariate.params['growth']` — wrong object | → `fit_bivariate.coef()['growth']` |
| ch16 | 1380 | `model_multiple.params.index` — statsmodels API on pyfixest | → `list(fit_multiple.coef().index)` |
| ch16 | 886, 1550 | Dead `smf.ols().fit()` creating unused `_diag_model` | Moved to correct location (before `OLSInfluence` calls) |

### HIGH Fixes (12) — Wrong syntax in exercise hints

Updated commented-out exercise hint code across ch01, ch14, ch15, ch16, ch17:
- `.params[...]` → `.coef()[...]` (7 occurrences)
- `.rsquared` → `._r2` (3 occurrences)
- `.bse` → `.se()` (2 occurrences)
- `.fittedvalues` → `.predict()` (2 occurrences)
- `.resid` → `._u_hat` (3 occurrences)
- `model_X.summary()` → `fit_X.summary()` (1 occurrence)

### MEDIUM Fixes (2) — Misleading comments

- ch14: Removed "Using statsmodels for detailed ANOVA table" comments (code uses pyfixest)
- ch07: Removed orphaned "Hypothesis Test Using statsmodels" section header

### Colab Notebooks Re-synced (6)

Regenerated .ipynb files for ch01, ch07, ch14, ch15, ch16, ch17 via `export_qmd_to_ipynb.py`.

### Confirmed Acceptable (no fix needed)

- ch12: `smOLS().fit(cov_type='HAC')` for intercept-only HAC SE — pyfixest requires a regressor
- ch17: `logit().fit()` for binary choice — pyfixest feglm not mature enough
- ch16: `OLSInfluence`, VIF, `het_white` — diagnostic tools only available in statsmodels
- Code summaries (`code/*.py`): `sm_ols` for F-test SSR extraction and diagnostics
- Web-app templates: All clean

## Files Modified

- `notebooks_quarto/ch01_*.qmd` — exercise hints
- `notebooks_quarto/ch07_*.qmd` — removed orphaned section header
- `notebooks_quarto/ch14_*.qmd` — comments + exercise hints
- `notebooks_quarto/ch15_*.qmd` — critical bug + exercise hints
- `notebooks_quarto/ch16_*.qmd` — critical bugs + dead code relocation + exercise hints
- `notebooks_quarto/ch17_*.qmd` — exercise hints
- 6 `.ipynb` Colab notebooks (auto-synced)
- 6 `.md` Markdown exports (auto-synced)
- 2 `book/_book/` HTML files (auto-synced)
- 2 `book/_freeze/` JSON files (auto-synced)
- 1 `book/_book/search.json` (auto-synced)
- 23 files total

## Project Status

The pyfixest migration is now fully complete and audited:
- **120 files** changed in initial migration (prior session)
- **23 files** fixed in this audit
- **0 known remaining issues** in .qmd notebooks
- All web-app templates: clean
- All code summaries: acceptable (statsmodels retained only for diagnostics)
- All Colab notebooks: synced
- All metadata (index.html, book/index.qmd, requirements.txt): correct
