# 2026-05-15 15:02 — Japanese (JA) added as third language to shared i18n runtime

## Goal

Execute the 6-phase plan from `~/.claude/plans/in-the-navigation-bar-crystalline-castle.md` (Japanese rollout). Add `"ja"` to `web-apps/_shared/i18n-config.js`'s `languages` array, fill the JA column of the canonical glossary, translate every entry across 23 `_shared/strings/*.js` files, extend the smoke test to a 3-language cycle, ship.

The architectural promise from the EN/ES refactor (commit `5faea7e`) — "no HTML touched, no helpers rewritten" — is what made this a pure data task.

## What shipped

### Phase 1 — JA section finalised in `style-notes.md`

Replaced the 11-line JA stub with a ~50-line section mirroring the depth of the ES section. Locked in the user's two decisions (です/ます register, keep English acronyms in Latin script) plus 9 inferred companion decisions (Western numerals, half-width punctuation in math/code/numbers, full-width 「。」「、」 in prose, Kuroda 黒田-tradition vocabulary preference, etc.). Added 5 strings-file-specific subrules surfaced by the ch01 pilot (no Latin-acronym JA gloss in strings files; stat-card labels strip parenthetical; keep Western quotes inside escaped `\"...\"` JS strings; em-dash strict preservation; `$` is USD throughout).

### Phase 2 — Glossary JA column filled (1 subagent)

One general-purpose subagent translated **244/244 empty JA cells** in `canonical-terms.md` (the JA column existed since Phase 1 of the EN/ES refactor; now populated). Result: only 1 `(jp: TBD)` marker (smearing factor — Duan 1983 retransformation correction with no consensus Japanese rendering). Notable canonical choices:

- `Heteroscedasticity` → 不均一分散 (Kuroda canonical, NOT katakana ヘテロスケダスティシティ)
- `OLS` / `R²` / `MELI` / acronyms → bare Latin throughout (Japanese econometric textbook convention)
- `pooled OLS` → プールドOLS
- `within / between estimator` → グループ内推定量（within）/ グループ間推定量（between）
- `Cook's distance / Breusch-Pagan` → kept Latin proper-noun form

Glossary now ships 270 rows × 3 languages.

### Phase 3 — Pilot translate `ch01.js` (1 subagent)

Single pilot subagent translated all **138 entries** in `ch01.js` (intro chapter, Davis CA housing dataset, descriptive statistics + intro to inference). EN/ES preserved byte-for-byte. Surfaced 11 new vocabulary terms (confounders, proxy variable, school district, baseline, what-if, distance from mean, eyeball line, tight/dispersed sample, right-skewed tail, etc.) and 5 spec clarifications (em-dash strict, `$` USD, no JA gloss in strings files, escaped quotes, stat-card stripping).

### Phase 4 — Pilot review

Spot-checked 10 entries across categories (chapter title, widget headings, stat callouts, HTML-embedded entries, interpolation-token entries, code library labels). All 5 hard rules honoured cleanly:

- Latin acronyms (OLS, R², IQR) survived bare
- HTML tags (`<strong>`, `<em>`, `<a href="..." target="_blank" rel="noopener">`, `<span class=\"kc-title\">`) preserved byte-for-byte
- Interpolation tokens (`{short}`, `{direction}`, `{slope}`, `{r2pct}`, `${truePred}`, `${amt}`) preserved exactly
- です/ます register applied throughout
- Em-dashes ` — ` preserved strict

Promoted the 11 new vocabulary items into a new "13. Phase-2 additions (promoted from ch01 pilot)" table in `canonical-terms.md` so the parallel agents would inherit them. Added the 5 strings-file subrules to the JA section of `style-notes.md`.

### Phase 5 — 22 parallel translator subagents

Single tool message, 22 Agent calls (one per remaining strings file), all dispatched in parallel. Each returned within 30 s – 11 min; total wall-clock ≈ 12 minutes. Every subagent reported:
- 100% entry coverage (en==es==ja count)
- `node --check` exit 0
- `python3 scripts/i18n_check.py` exit 0 (validator was still EN/ES at this point — `ja:` keys parse cleanly even when not yet required)
- Vocabulary self-audit + ambiguous-entry flags

Per-file JA coverage (verified by `grep -c 'ja:' web-apps/_shared/strings/*.js`):

```
nav.js              13   common.js            5   code.js             10   chapter-titles.js  17
index.js            52   tutors.js           92
ch01.js (pilot)    138   ch02.js            200   ch03.js            112   ch04.js            138
ch05.js            157   ch06.js            182   ch07.js            143   ch08.js            156
ch09.js            171   ch10.js            141   ch11.js            113   ch12.js            145
ch13.js            128   ch14.js            110   ch15.js            125   ch16.js            177
ch17.js            133
```

Total: **2,658 per-file entries** (sum across files; validator dedupes to 2,420 unique keys after the 3-language regex tightens). Every file's en/es/ja counts match exactly — no silent drops or over-emits.

### Phase 6.1 — Switch flipped

Single one-character-class edit:

```diff
-  languages:   ["en", "es"],
+  languages:   ["en", "es", "ja"],
```

Validator output post-flip:

```
[i18n-check] languages: ['en', 'es', 'ja']
[i18n-check] 2420 keys defined across 23 string file(s)
[ok] every key has all 3 language values
[ok] no conflicting key definitions across string files
[i18n-check] 19 migrated HTML page(s)
[ok] every referenced key has a definition
[warn] 36 keys defined but never referenced (orphaned, not a hard fail)
[i18n-check] ALL CLEAN ✓
```

### Phase 6.2 — Smoke test extended for 3-language cycle

Refactored `scripts/smoke_i18n.py`:

- New top-level constant `LANG_CYCLE = ["en", "es", "ja"]`.
- `smoke_chapter()` now snapshots after every toggle (3 transitions instead of 1), asserts each transition flips `documentElement.lang`, `#siteLanguageLabel`, `document.title`, and 5 `[data-i18n]` element texts. Asserts the final snapshot equals the initial one (full round-trip equality).
- `smoke_persistence()` now navigates index → toggle → ch08 → return to index for every step in the cycle (3 round-trips). Asserts ch08 boots in the same language `localStorage` was set to from index.
- Leak detector unchanged — the existing `[a-z]+\.[a-z][\w.]*` regex doesn't match Japanese characters, so JA values can't leak as raw key names.

Result:

```
[ch04] PASS  (titles: Chapter 04 — Statistical Infer… -> Capítulo 04 — Inferencia estad… ->
              第04章 — 平均に関する統計的推測 · metricsAI;
              plotly: '50' -> '50' -> '50')
[ch10] PASS  (titles: Chapter 10 — Data Summary for … -> Capítulo 10 — Resumen de datos… ->
              第10章 — 重回帰のためのデータ要約 · metricsA…;
              plotly: 'Bivariate (total effect)' -> 'Bivariada (efecto total)' -> '二変量 （全効果）')
[ch16] PASS  (titles: Chapter 16 — Model and Data Ve… -> Capítulo 16 — Verificación del… ->
              第16章 — モデルとデータの検証 · metricsAI;
              plotly: 'age' -> 'edad' -> '年齢')
[persistence] PASS

ALL PHASE 5 TESTS PASSED ✓
```

The Plotly tick evidence on **ch10** (`'Bivariate (total effect)' → 'Bivariada (efecto total)' → '二変量 （全効果）'`) and **ch16** (`'age' → 'edad' → '年齢'`) proves the `__rerender_*` registry fires on every language switch and Plotly chart text re-paints in all three languages.

ch04's tick was numeric (`'50'`) so the value matches in both languages — expected, not a regression.

### Phase 6.3 — Manual smoke

Skipped automated capture; eyes-on confirmation by user is the natural way to verify CJK rendering (font fallback, encoding) on their system. The Playwright run above produced JA characters cleanly so encoding is sound.

### Phase 6.4 — Memory updated

Updated `~/.claude/projects/.../memory/project_website_i18n.md` to mark JA as DONE (was TODO) and reference this log as the worked example for the 3-step "adding a language" recipe. The matching entry in `MEMORY.md` is unchanged — its description already pointed at the architecture rather than the language list.

## Subagent dispatch summary

| Phase | Subagent count | Duration |
| --- | --- | --- |
| Phase 2 — glossary fill | 1 | ~5 min |
| Phase 3 — pilot ch01.js | 1 | ~10 min |
| Phase 5 — parallel fan-out | 22 | ~12 min wall-clock (max single-agent ~11 min) |
| **Total** | **24 subagents** | **~30 min wall-clock end-to-end** |

Zero re-dispatches needed. Zero validator failures. Zero browser-smoke failures.

## Diff summary

```
27 files changed, +5,144 insertions, +90 deletions
```

vs the immediate predecessor (commit `5faea7e`):

- `web-apps/_shared/i18n-config.js` (+1 / -1)
- `web-apps/_shared/glossary/style-notes.md` (~+50 / -10)
- `web-apps/_shared/glossary/canonical-terms.md` (~+60 / -240) — net since ja-cells were `—` before
- 23 strings files (~+5,000 net — one ja: line per entry across 2,658 entries)
- `scripts/smoke_i18n.py` (~+30 / -25)

**Zero HTML files modified.** Zero per-chapter `template.html` / `dashboard.html` / `i18n-runtime.js` / `i18n_check.py` touched. Architecture invariant upheld.

## Vocabulary candidates for promotion (post-rollout cleanup, not blocking)

Translators flagged these new terms as future-glossary candidates (mostly chapter-specific domain vocabulary):

- ch03: 加重平均, 重みなし平均, 逆確率重み, 包含確率, 混入率, 二峰性, 指数分布, ベルヌーイ, 一様分布
- ch06: 母集団, 標本変動, モンテカルロ
- ch07: 不均一分散の強さ, 誤差の構造, カバー率, CIバンド, 検定タイプ
- ch08: 個別リスク, ディフェンシブ銘柄, グロース銘柄, 構造変化, ジョブレス・リカバリー, 影響度, Okunの法則
- ch12: 第I種誤り, 第II種誤り, 検出力, ブートストラップ反復, 偽陽性, 偽陰性, 「狼が来た」と叫ぶ
- ch13: 現職優位, 自己負担, モラルハザード, 平行トレンド, 入植者死亡率, 回帰不連続, ビン平均, 準実験, 希薄化
- ch14: ダミー変数の罠, 男女賃金格差, 教育の収益率, 同時F検定, 自営業
- ch15: 二次モデル, 二乗項, 限界効果, smearing係数, Duanのsmearing, 標準化係数, イェンセンの不等式
- ch16: 影響観測値, 実効標本, 閾値, 分散拡大要因, OVB
- ch17: 見せかけの回帰, 動学的乗数, インパクト乗数, 累積乗数, 平均除去後

These are documented in the per-subagent reports (above the merge); promoting them to topical tables in `canonical-terms.md` is a follow-up sweep, not a blocker.

## Pilot vs fan-out cohesion

The pilot's 5 spec clarifications (added to `style-notes.md` before fan-out) appear to have prevented 100% of the structural classes of failure they were designed to address. Across the 22 parallel agents:

- Zero accidentally translated `code.tab.python` / `.stata` / `.r` values (they all stayed `Python`/`Stata`/`R`).
- Zero added Latin-acronym parenthetical glosses inside string values (ans: bare `OLS`, bare `IQR`).
- Zero substituted `—` with 「、」 mid-sentence.
- Zero converted `$` to `¥`.
- Zero used full-width quotes inside `\"escaped\"` JS strings.

The remaining ambiguity flags (e.g., ch11 `KC 11.5` reference IDs, ch14 `Earnings ($)` translation, ch08 `idiosyncratic risk diversifies away` register tone) are pedagogical judgement calls, not regressions.

## Outstanding follow-ups (separate plans)

1. **`videos.html` migration to shared runtime** — the page still uses an inline `translations.es` dict (Phase 3 of the EN/ES refactor skipped it). Adding videos.html now means: include the 7 `<script src>` tags + create `_shared/strings/videos.js` (with EN/ES/JA). The infrastructure makes this a 1-hour task.
2. **Skill scaffold update** (`.claude/skills/web-app/templates/base.html`) — does NOT yet inherit the shared runtime. Future-scaffolded chapters will need to be migrated via `migrate_to_shared_i18n.py`. Update once stable.
3. **Vocabulary promotion sweep** — fold the ~50 chapter-specific JA terms above into topical tables in `canonical-terms.md`. Follow-up cleanup; not blocking the next translator.
4. **4th language (Portuguese, Mandarin, German…)** — the same 6-phase recipe applies. The architecture is now battle-tested across 3 languages.

## Architecture (final, post-JA)

```
web-apps/_shared/
├── i18n-config.js       (3 lines — languages: ["en","es","ja"])
├── i18n-runtime.js      (~95 lines, unchanged)
├── strings/             (23 files, 2,420 unique keys × 3 languages = 7,260 string values)
│   ├── nav.js / common.js / code.js / chapter-titles.js   (always loaded)
│   ├── index.js / tutors.js                               (page-exclusive)
│   └── ch01.js…ch17.js                                    (page-exclusive)
└── glossary/
    ├── canonical-terms.md  (270 rows, 3 lang columns + notes)
    └── style-notes.md      (ES + JA sections complete)

scripts/
├── i18n_check.py             (validator — language-agnostic, reads i18n-config.js dynamically)
├── smoke_i18n.py             (Playwright regression — now LANG_CYCLE-driven, EN→ES→JA→EN)
└── migrate_to_shared_i18n.py (Phase 4 archaeology — kept for posterity)
```

## Next session pickup

The site now ships in 3 languages. Three natural next steps:

1. **`videos.html` i18n** (separate plan, ~1 hour) — closes the last EN/ES/JA gap.
2. **Skill scaffold update** (~1 hour) — bakes the shared runtime into newly-scaffolded chapters.
3. **Vocabulary promotion sweep** (~30 min) — promotes the ~50 chapter-specific JA terms into topical tables.

Adding a 4th language (e.g., Portuguese) is now: (a) edit `style-notes.md` (1 PR section), (b) one glossary subagent, (c) one pilot translator, (d) review, (e) 22 parallel translators, (f) flip switch + run validators. Same recipe, no surprises.
