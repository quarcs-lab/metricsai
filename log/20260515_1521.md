# 2026-05-15 15:21 — Project status snapshot

End-of-day status across all major workstreams. This log is a snapshot, not a session-of-work log — for detailed step-by-step work see the session logs from earlier today (`20260515_1244.md` … `20260515_1502.md`).

## Headline state

The site ships in **3 languages** (EN / ES / JA) across 19 pages. All chapter content (CH00–CH17) is at 90+ standardisation score. PDF generation pipeline is green. Quarto book is published to GitHub Pages.

## Workstream status

### 1. Chapters (`notebooks_quarto/`, `notebooks_colab/`, `notebooks_md/`, `book/`)

| Status | Detail |
| --- | --- |
| ✅ Source of truth | 18 `.qmd` files in `notebooks_quarto/` (ch00–ch17) |
| ✅ Colab notebooks | 18 `.ipynb` files auto-synced via `scripts/export_qmd_to_ipynb.py` |
| ✅ Markdown mirror | 18 `.md` files in `notebooks_md/` (auto-sync hook) |
| ✅ Quarto HTML book | Rendered to `book/_book/`, published at `quarcs-lab.github.io/metricsai/book/_book/` |
| ✅ Standardisation | All CH01–CH17 at 90+ score (per `chapter-standard` skill verifier). CH00 (Preface) excluded |
| ✅ Key Concepts glossary | All CH01–CH17 carry the new `## Key Concepts` glossary section (~140 entries total, see `log/20260508_1457.md`) |
| ✅ Case studies | Mendez convergence-clubs + DS4Bolivia datasets integrated into 12+ chapters each |

### 2. PDFs (`notebooks_colab/*.pdf`)

| Status | Detail |
| --- | --- |
| ✅ Per-chapter PDFs | ch00–ch17, 0.9–2.0 MB each, 15–72 pages each (last regenerated 2026-02-08) |
| ✅ Compiled book PDF | `notebooks_colab/metricsAI_complete_book.pdf` (62.3 MB, 900 pages, 132 bookmarks, 753 clickable TOC links) |
| ✅ Pipeline | `quarto render` → `inject_print_css.py` → `generate_pdf_playwright.py` |

### 3. Web apps (`web-apps/chNN/`)

| Status | Detail |
| --- | --- |
| ✅ 17 chapter dashboards | `web-apps/ch01`–`ch17/dashboard.html` — single-file Plotly.js dashboards |
| ✅ Site navbar rollout | All 17 dashboards share the site nav (commit history; migration script `scripts/migrate_webapp_navbar.py`) |
| ✅ Shared i18n runtime | Phases 1–6 complete (commits `f159eb8` … `5faea7e`). Net diff: −10,912 lines across 65 files |
| ⏳ `videos.html` migration | Still uses inline `translations.es` pattern. Pre-shared-runtime. Follow-up |
| ⏳ Skill scaffold | `.claude/skills/web-app/templates/base.html` does NOT yet inherit shared runtime — newly-scaffolded chapters require `migrate_to_shared_i18n.py` step |

### 4. i18n (`web-apps/_shared/`)

| Status | Detail |
| --- | --- |
| ✅ Runtime | `i18n-config.js` (3 lines), `i18n-runtime.js` (~95 lines) |
| ✅ String tables | 23 files × ~2,420 unique keys × 3 languages = ~7,260 string values |
| ✅ Languages | EN (default), ES (commit `5faea7e`), JA (commit `01e4c5f`) |
| ✅ Glossary | 270 rows × EN/ES/JA columns + 13-row Phase-2 additions table. 1 `(jp: TBD)` (smearing factor — Duan 1983) |
| ✅ Style notes | ES + JA sections complete. Subagent prompt template at bottom |
| ✅ Validators | `scripts/i18n_check.py` (parity gate), `scripts/smoke_i18n.py` (Playwright 3-language regression) |
| ⏳ Vocabulary promotion | ~50 chapter-specific JA terms surfaced by parallel translators await promotion to topical glossary tables (cleanup, non-blocking) |

### 5. AI Tutors (`tutors/`)

| Status | Detail |
| --- | --- |
| ✅ 9 tutor instruction files | Customized Google Gemini Gems |
| ✅ Tutors page | `tutors.html` migrated to shared runtime + JA-localized |
| ✅ Featured tutors live | Learning Coach, Socratic Challenger, Code-First Experimenter, Exam Coach, Analogy Builder |
| 🚧 Forthcoming | Debate Partner, Visual Intuition, Research Mentor, Spaced Review |

### 6. Code summaries (`code/`)

| Status | Detail |
| --- | --- |
| ✅ Per-chapter Python summaries | `code/chNN_*.py` (17 files) |
| ✅ Compiled reference | `code/short/codeSummary.md` |

### 7. Docs

| Status | Detail |
| --- | --- |
| ✅ `CLAUDE.md` | Updated this session — adds `_shared/` to project structure, i18n commands, architecture bullet |
| ✅ `README.md` | Updated this session — trilingual interface callout in Learning Apps section |
| ✅ Skills | 7 skills under `.claude/skills/`: chapter-standard, compile-book, proofread, improve-readability, improve-webapp-readability, web-app, create-chapter-code-summary, add-key-concepts-section |
| ✅ Workflow rules | `.claude/rules/pdf-generation.md`, `.claude/rules/quarto-book.md` |

## What changed today (2026-05-15)

8 commits across the EN/ES infrastructure refactor + JA rollout:

```
f159eb8  Add shared i18n runtime + canonical glossary (Phase 1)
1e108f6  Add shared i18n string tables (Phase 2)
73bbb93  Migrate index.html + tutors.html to shared i18n runtime (Phase 3)
a293cdb  Migrate 17 chapter dashboards to shared i18n runtime (Phase 4)
5faea7e  Verify shared i18n runtime end-to-end (Phases 5-6)
01e4c5f  Add Japanese (JA) as third language to shared i18n runtime
(this commit) Document trilingual i18n in CLAUDE.md + README.md + status snapshot log
```

Cumulative diff vs `ffed885^` (the commit before today's work began):

- **−7,621 net lines** across the EN/ES dedup + JA addition
- 65 files in the EN/ES refactor (mostly deletions in chapter dashboards)
- 28 files in the JA rollout (additions, no deletions outside the language array)
- Architecture invariant held across both refactors: **zero per-chapter HTML rewritten by hand**

## Active follow-ups (non-blocking)

1. **`videos.html` i18n migration** (~1 hour) — last page using the old inline pattern.
2. **Skill scaffold update** — bake the shared runtime into `.claude/skills/web-app/templates/base.html` so new chapters inherit it from day one.
3. **Vocabulary promotion sweep** (~30 min) — fold ~50 chapter-specific JA terms into topical tables in `canonical-terms.md`.
4. **Dead chapter keys cleanup** — 36 orphan keys flagged by `i18n_check.py` (e.g., `common.takeaway` in 13 chapters never referenced).
5. **4th language (PT, ZH, DE…)** — 6-phase recipe documented in `log/20260515_1502.md`. Wall-clock estimate: ~30 minutes with subagent fan-out.

## Pinned commands

```bash
# i18n parity check (gate)
python3 scripts/i18n_check.py

# i18n browser smoke (regression — 3 chapters × 3 languages + cross-page persistence)
python3 scripts/smoke_i18n.py

# Render Quarto book
cd book && quarto render

# Generate all chapter PDFs
python3 scripts/generate_pdf_playwright.py --all

# Compile single-file book PDF
python3 scripts/compile_book.py
```

## Health check (as of this snapshot)

```
$ python3 scripts/i18n_check.py
[i18n-check] languages: ['en', 'es', 'ja']
[i18n-check] 2420 keys defined across 23 string file(s)
[ok] every key has all 3 language values
[ok] no conflicting key definitions across string files
[i18n-check] 19 migrated HTML page(s)
[ok] every referenced key has a definition
[i18n-check] ALL CLEAN ✓

$ python3 scripts/smoke_i18n.py
[ch04] PASS  (en→es→ja round-trip clean)
[ch10] PASS  (Plotly tick: 'Bivariate (total effect)' → 'Bivariada (efecto total)' → '二変量 （全効果）')
[ch16] PASS  (Plotly tick: 'age' → 'edad' → '年齢')
[persistence] PASS
ALL PHASE 5 TESTS PASSED ✓
```

The site is shippable in the current state.
