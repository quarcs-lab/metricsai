# Judgment-Call Report (Phase 1 seed) — action=report findings

These need the author decision. Grouped by chapter.

**Total report items: 191**

## ch00 (2 report items)

- **ch00-F06** [results/error] L92-94: The 'Modern Python Stack' description is factually inaccurate: it credits Linearmodels (never imported anywhere, absent from requirements.txt) as a core library and omits pyfixest, which is the actual regression engine used in 14 chapters.
- **ch00-F08** [format/style] L116-234: 'Gemini PRO' (all-caps PRO) is nonstandard branding for Google's 'Gemini Pro' model; appears three times (lines 116, 158, 234).

## ch01 (4 report items)

- **ch01-F03** [methods/error] L369-371: Section 1.7 instructs students to read an 'F-statistic (top right section)' and its 'Prob F-statistic', but pyfixest's summary reports no F-statistic at all, so the bullet points at output that does not exist; remedy requires choosing among deleting the bullet, replacing it with the RMSE line that pyfixest does print, or noting F = t² for the bivariate case — a pedagogical choice beyond a one-sentence fix.
- **ch01-F12** [code/error] L162-205: The Setup cell imports pyfixest with no install step, so the exported Colab notebook's very first cell likely fails with ModuleNotFoundError in a fresh Colab session (pyfixest is not in Colab's default image), contradicting the front-matter claim at L15 that 'You can run all code directly in Google Colab without any local setup required'.
- **ch01-F20** [results/style] L133: R-squared analogy says the forecast 'captures roughly two-thirds' (66.7%) while the actual R² is 61.75%, an 8% relative gap beyond the 5% tolerance for 'roughly' claims and inconsistent with the sibling Example panel's 'about 62%'.
- **ch01-F21** [interpretation/style] L376-377: The headline slope coefficient is interpreted for magnitude and R-squared but its statistical significance (t = 6.60, p < 0.001) is never discussed in prose, despite KC 1.5 and the Key Takeaways (L474) both naming significance as a key element of interpretation.

## ch02 (4 report items)

- **ch02-F13** [methods/style] L1507: Exercise 4 asks students to approximate the percentage change for a 0.5 log increase, a magnitude at which the delta-ln approximation (50%) is far from the exact answer (64.9%), with no prompt to confront the exact formula.
- **ch02-F18** [code/error] L1939-1995: Task 6 instructs students that they must 'manually assign regions based on country names' and build a ~50-entry region_mapping dictionary, but the Mendez dataset already contains a 'region' column (Africa/Americas/Asia/Europe/Oceania), and the hint's taxonomy (Middle East, Asia-Pacific) plus example countries ('Nigeria', 'USA') do not match the actual data (no Nigeria; the US appears as 'United States').
- **ch02-F23** [code/style] L340-1397: Deprecated matplotlib boxplot kwargs are used in five places: vert=False at L340 (main box plot cell) and L1397 (cheat sheet), vert=True at L1765 (Task 3 template), labels= at L1843 (Task 4 hint; renamed tick_labels since 3.9), and vert=False in the commented hint at L2182 — all currently run but will break when removed (vert removal announced for matplotlib 3.13).
- **ch02-F54** [interpretation/style] L1017-1021: The 'variance stabilization' argument compares SD-to-mean ratios across the log transformation (62% vs 6%), but the coefficient of variation of a log-scale variable is not unit-invariant, so the 6% figure is not meaningful evidence of stabilization.

## ch03 (7 report items)

- **ch03-F09** [methods/style] L1413-1415: Case-study Task 2 starter code samples without replacement, setting the pattern students carry into Tasks 3-6 where independence is required for the σ/√n theory being verified; align with the Task 3 fix.
- **ch03-F11** [methods/style] L1493-1501: Inconsistent standard-error definition: Key Concept 3.11 (and KC 3.10 L1327, plus prose at L645 and L1010) define/use SE(ȳ) = σ/√n, while Key Concept 3.4 (L534-536) and Key Takeaways L1006-1008 define the standard error as the ESTIMATED sd s/√n and state that σ/√n is a different (unknown) quantity.
- **ch03-F14** [code/error] L1461-1482: Case-study Tasks 2-4 sample WITHOUT replacement from a population of only N=108 countries while the starter code and questions compare the empirical SE to σ/√n, which omits the finite-population correction, so Task 3's comparison question fails by ~16% at n=30 and Task 4's 'verify SE decreases as 1/√n' fails by ~73% at n=100.
- **ch03-F21** [results/error] L952-968: The executed weighted-mean demo contradicts its own printed labels: the 'corrected' weighted mean ($54,647, bias +$1,204) lands over 5× farther from the true population mean ($53,442) than the mean labeled '(biased toward women)' ($53,222, bias −$221), undermining the section's lesson that IPW weighting fixes nonrepresentative-sample bias.
- **ch03-F23** [interpretation/error] L1461-1514: Case-study Tasks 3-4 tell students to compare the empirical SE against the 'theoretical prediction' sigma/sqrt(n) and to 'Verify that SE decreases as 1/sqrt(n)', but the starter code samples WITHOUT replacement from a population of only 108 countries, so the finite-population correction makes the comparison systematically fail (empirical SE ~16% below sigma/sqrt(n) at n=30 and ~73% below at n=100).
- **ch03-F27** [interpretation/drift] L684-725: The '### Interpreting the Simulation Results' block sits in §3.4 (census section, right after Key Concept 3.6) but interprets output from the §3.6 Monte Carlo simulation cell that appears ~140 lines later, so readers meet 'Key findings from 400 simulated coin toss samples' (mean 0.5004, SD 0.0887, seed 10101) before any simulation has run; the block should be moved into §3.6 after the simulation cell/figure (L828-879), which currently has no interpretation of its own.
- **ch03-F29** [readability/error] L684-725: The entire '### Interpreting the Simulation Results' block sits in Section 3.4 (census example) but interprets the Monte Carlo simulation that is only run later in Section 3.6 — readers meet numeric conclusions (mean 0.5004, SD 0.0887, range 0.2667-0.7667, seed 10101, comparison to AED_COINTOSSMEANS.DTA) before the code that produces them exists; the block should be relocated to Section 3.6, directly after the simulation cell that ends at line 852 (before the 'This figure shows...' sentence at line 855), which also fixes the simulation cell's currently missing post-output interpretation.

## ch04 (1 report items)

- **ch04-F26** [results/error] L1602-1602: Exercise 8(d)'s stated answer is wrong: with data generated from N(50, 100), the null H₀: μ = 55 is false, so the proportion of rejections is the test's power (≈0.999 if 100 is the variance, ≈0.08 if 100 is the sd), not ≈0.05.

## ch05 (7 report items)

- **ch05-F07** [methods/drift] L1925: KC 5.9 writes the slope as β₁ in 'productivity = β₀ + β₁ × capital', contradicting the chapter's own convention where b₁ is the intercept and b₂ the slope, and where β₁/β₂ denote the true (population) intercept/slope.
- **ch05-F15** [code/error] L1079-1079: Standardized residuals divide by fit._u_hat.std() (numpy ddof=0, i.e. sqrt(RSS/n) = $22,724), which contradicts the chapter's own definition of s_e = sqrt(RSS/(n-2)) = $23,551 given at L757 and computed as `se` at L769.
- **ch05-F19** [code/error] L2058-2086: Case Study 2 Task 1 instructions say to bin imds into 'three equal-frequency groups ... (terciles)' but the code uses pd.cut(bins=3), which makes equal-WIDTH bins — the executed groups are 170/157/12, nowhere near terciles.
- **ch05-F44** [interpretation/error] L2058-2080: Case Study 2 Task 1 tells students the imds bins are 'three equal-frequency groups ... (terciles)' and the code comment says 'Bin imds into terciles', but pd.cut(bins=3) creates equal-WIDTH bins — executed groups are Low=170, Medium=157, High=12 of 339, so readers will misinterpret the crosstab as comparing similar-sized groups.
- **ch05-F45** [interpretation/error] L1090-1091: The outlier cell's label 'Top 5 largest residuals (in absolute value)' misdescribes the displayed table: nlargest(5, 'residual') sorts signed values, so the observation with the 2nd-largest absolute residual (obs 4: size 2,100, price $224,500, residual about -$45,436) is silently omitted and readers conclude all big misses are positive.
- **ch05-F53** [readability/style] L343-363: Section 5.2 ends with two overlapping wrap-up blocks — 'Limitation of tabulation' + 'Next step' (L343-349) and 'From Categorical to Continuous' (L351-363) — that restate the same three limitations (information loss, can't quantify strength, no precise prediction) and the same 'use correlation and regression' conclusion.
- **ch05-F60** [readability/style] L945-960: The 'Understanding Prediction Uncertainty' block sits in section 5.7 before the fit.summary() cell, but it discusses the 2,000 sq ft prediction (ŷ = $262,559) and prediction intervals — material that is only computed and introduced in section 5.8; it also separates the 5.7 lead-in from the cell it introduces.

## ch06 (21 report items)

- **ch06-F01** [methods/error] L250-262: Section 6.2 promises two sampling examples ('1. Generated data... 2. Census data: Samples from the 1880 U.S. Census'), and the datasets bullet (L50) and Key Takeaways (L889-890, '1.06 million males aged 60-70') repeat the claim, but the chapter contains no census example at all — no code cell, figure, or discussion beyond these mentions.
- **ch06-F02** [methods/error] L293-342: Headings 'Figure 6.2 Panel A/B' and two full interpretation blocks discuss regression output and figures that no code cell produces: the population regression of Eygivenx on x ('Intercept coefficient = 1.0000... R² = 1.000... Standard errors ≈ 0') and the sample regression 'ŷ = 2.81 + 1.05x' with R² 0.769 are never estimated or plotted in the chapter.
- **ch06-F03** [methods/error] L503-635: The chapter's central demonstration — 'we simulate 1,000 regressions' with detailed results (intercept mean 0.9960, SD 1.2069; slope mean 1.9944, SD 0.3836) and the sampling-distribution histograms ('Green vertical line', 'Red curve: Normal distribution fit') — has no code cell anywhere in the chapter; two long interpretation sections analyze output that Colab and book readers never see.
- **ch06-F04** [methods/error] L668-751: The 'Example: Manual Computation of Standard Errors' section walks through Steps 1-5 of calculations and claims 'Verification: Our manual calculations match the model output exactly' (L737), but no code cell performs the manual computation or fits the model, so there is nothing for readers to verify against.
- **ch06-F05** [methods/error] L1310-1387: The case-study Monte Carlo samples n=50 (and Task 5: n=20/50/80; Task 6: n=30 of 54) WITHOUT replacement from a 108-country 'population' (template L1331: sample(n=sample_size, replace=False)), so the finite-population correction makes the empirical sampling SD shrink much faster than the 1/√n law that Task 5 asks students to verify, and Task 4's claim that theoretical and empirical SEs 'should be close' fails.
- **ch06-F22** [code/error] L503-636: The Monte Carlo simulation of 1,000 regressions is extensively narrated with hand-typed numeric results (intercept mean 0.9960/SD 1.2069, slope mean 1.9944/SD 0.3836) and described histograms (green true-value line, red normal curve), but no code cell runs the simulation or draws the histograms anywhere in the chapter.
- **ch06-F23** [code/error] L293-342: Headers 'Figure 6.2 Panel A: Population Regression Line' and 'Figure 6.2 Panel B: Sample Regression Line' plus two 'Interpreting the ... Regression Results' sections narrate regression output (Intercept=1.0000, Slope=2.0000, R²=1.000; ŷ=2.81+1.05x, R²=0.769) that no code cell produces — neither regression is estimated and neither figure is plotted.
- **ch06-F24** [code/error] L668-751: The 'Example: Manual Computation of Standard Errors' section says 'Let's manually compute standard errors', walks through 'the observation-level calculations', and claims 'Verification: Our manual calculations match the model output exactly' — but contains no code cell computing residuals, RSS, sₑ, se(b₂), or any model output.
- **ch06-F25** [code/error] L255: Example 2, 'Census data: Samples from the 1880 U.S. Census (a finite population)', is announced in §6.2 (and in 'Datasets used' at L50 and Key Takeaways at L889: 'Samples from 1880 Census (1.06 million males aged 60-70)') but no census data is ever loaded and no census example exists anywhere in the chapter.
- **ch06-F34** [results/drift] L517-635: The Monte Carlo section hand-types specific simulation results (intercept mean 0.9960, SD 1.2069; slope mean 1.9944, SD 0.3836; echoed as 'SD ≈ 0.38' in Key Concept examples at L116/L131 and L101) but the chapter contains no Monte Carlo code cell, so these numbers appear in no executed output and cannot be verified or reproduced by readers.
- **ch06-F35** [results/drift] L737-741: The manual standard-error section claims 'Our manual calculations match the model output exactly', but the chapter contains no code cell performing the manual calculation and no model output for this example, so there is nothing for the reader to verify the claim against.
- **ch06-F36** [results/drift] L889-889: Key Takeaways claims the chapter presented 'Two examples: ... (2) Samples from 1880 Census (1.06 million males aged 60-70)', and the 1880 Census is also promised at L50 (Datasets used) and L255 (section 6.2 intro), but no Census example or data load exists anywhere in the chapter, and the '1.06 million males aged 60-70' figure has no source in any executed output.
- **ch06-F39** [interpretation/error] L1347-1383: Tasks 4-5 tell students the theoretical SE and the Monte Carlo SD 'should be close' and that se(b2) 'decreases roughly as 1/sqrt(n)', but under the prescribed design (sampling n=20/50/80 WITHOUT replacement from a population of only 108 countries, per Task 3 code replace=False at L1331) both claims fail quantitatively, so students following the instructions will observe results that contradict the stated expected outcomes.
- **ch06-F40** [interpretation/error] L252-255: The chapter promises and even summarizes results from an 1880 U.S. Census finite-population sampling example ('We examine two examples', L252-255; front-matter dataset bullet L50; Key Takeaways L890 cites 'Samples from 1880 Census (1.06 million males aged 60-70)'), but no Census data, code, or output exists anywhere in the chapter, so readers are told about findings from a demonstration they never see.
- **ch06-F41** [interpretation/error] L293-751: Five interpretation sections analyze specific numeric outputs from code cells that do not exist in the chapter: 'Figure 6.2 Panel A/B' headings (L293, L314) promise figures never rendered; 'Interpreting the Population Regression Results' (L299) cites coefficients/R2/SEs from an absent regression; 'Interpreting the Sample Regression Results' (L318) discusses y-hat = 2.81 + 1.05x and R2 = 0.769 with no fitting cell; 'Interpreting the Monte Carlo Simulation Results' (L517) and 'Interpreting the Sampling Distribution Histograms' (L580) report unreproducible simulation numbers (means 0.9960/1.9944, SDs 1.2069/0.3836) with no simulation cell or seed; and 'Interpreting the Manual Standard Error Calculations' (L677) claims 'Our manual calculations match the model output exactly' (L737) when neither calculations nor model output appear.
- **ch06-F42** [readability/error] L293-342: Sections 'Figure 6.2 Panel A' and 'Panel B' plus their two 'Interpreting the ... Results' blocks discuss regression output (intercept 1.0000, R²=1.000; ŷ = 2.81 + 1.05x, R²=0.769) that no code cell in the chapter produces — readers are told to 'Notice' numbers they never see.
- **ch06-F43** [readability/error] L503-561: 'Simulation: Sampling Distribution of OLS Estimator' (L503) and 'Interpreting the Monte Carlo Simulation Results' (L517–561) present detailed results of a 1,000-replication Monte Carlo (mean intercept 0.9960, SD 1.2069; mean slope 1.9944, SD 0.3836) but the chapter contains no simulation code cell — the numbers are unverifiable by the reader and unreproducible in Colab.
- **ch06-F44** [readability/error] L565-636: 'Visualization: Sampling Distributions of OLS Estimators' and 'Interpreting the Sampling Distribution Histograms' (L565–636) describe and interpret histograms ('Green vertical line', 'Red curve', intercept SD 1.21 vs slope SD 0.38 panels) that are never plotted — no plotting cell exists for these figures.
- **ch06-F45** [readability/error] L668-751: 'Example: Manual Computation of Standard Errors' walks through Steps 1–5 of a calculation and even claims 'Verification: Our manual calculations match the model output exactly' (L737), yet no code cell performs the manual computation or fits the model — neither the calculations nor the model output appear anywhere.
- **ch06-F46** [readability/error] L253-255: The chapter promises a second sampling example using the 1880 U.S. Census ('Datasets used' L50, numbered list item L255, Key Takeaways L889 'Samples from 1880 Census (1.06 million males aged 60-70)') but no Census section, data load, or code exists anywhere in the chapter.
- **ch06-F47** [readability/style] L350-382: 'Interpreting the Three Sample Regressions' (with the results table 0.82/1.81, 1.75/1.79, 2.01/1.67) appears BEFORE the code cell at L392 that generates those samples — interpretation precedes the output it interprets, inverting the lead-in → code → interpretation order used elsewhere.

## ch07 (8 report items)

- **ch07-F06** [methods/style] L850-851: The computed t-statistic formula writes the unknown population parameter σ_u in the denominator, contradicting the chapter's own emphasis that the computed t uses the estimate s_e (which is precisely why T(n−2) replaces N(0,1)).
- **ch07-F07** [methods/style] L2491-2628: Case Study 2 switches notation and calls the slope on ln_NTLpc2017 'β₁' (Tasks 1, 4, and 5: H₀: β₁ = 0, H₀: β₁ = 5, H₀: β₁ ≤ 0), whereas the entire rest of the chapter reserves β₁ for the intercept and β₂ for the slope.
- **ch07-F41** [interpretation/error] L1854-1864: Exercise 8's stated sample averages (average firm spends $10,000 on advertising, i.e. x̄=10, and has $5 million in sales, ȳ=5) are mathematically inconsistent with the given OLS output (intercept 2.5, slope 3.2), since the fitted line must pass through (x̄, ȳ): 2.5 + 3.2×10 = 34.5 ≠ 5, and the implied economic magnitude ($3.2M extra sales per $1,000 of advertising for a $5M firm) is incoherent for the part (d) economic-significance discussion.
- **ch07-F48** [readability/drift] L626-628: The heading '### Example with Artificial Data' and its one-sentence lead-in (L626-628) are stranded roughly 100 lines before the code cell they introduce (L727), separated by the entire §7.4 opening and Key Concepts 7.3-7.5; the block should be moved to immediately precede the '# Example with artificial data' cell.
- **ch07-F49** [readability/drift] L1106-1365: A ~260-line robust-standard-errors lecture ('Why Robust Standard Errors Matter' + 'Comparing Standard vs Robust Standard Errors: A Practical Guide') sits inside §7.5 Two-Sided Hypothesis Tests, before §7.6, even though robust SEs are the topic of §7.7; relatedly Key Concept 7.6 (heteroskedasticity, L1025-1051) sits under §7.4.
- **ch07-F66** [format/drift] L625-629: H3 '### Example with Artificial Data' at L625 is a stranded, contentless section: its one-sentence lead-in is immediately followed by '## 7.4', while the artificial-data code cell it introduces actually appears later inside section 7.4 (L727-749).
- **ch07-F96** [format/style] L496-506: Sub-bullets indented with only 1 space under numbered list items render as separate flat top-level lists instead of nested sub-lists (numbering survives via start attributes, but nesting is lost).
- **ch07-F97** [format/style] L1947-2622: Case-study Task headings use H3 ('### Task N ...'), making each task a sibling of its parent '### Case Study N' heading rather than a child; Case Study 2 also mixes an H4 ('#### Load the DS4Bolivia Data', L2460) between H3 siblings, so the heading hierarchy is inconsistent.

## ch08 (13 report items)

- **ch08-F02** [methods/error] L269-283: The 'Robust Standard Errors' subsection declares robust SEs standard for this cross-sectional setting, yet the life-expectancy regression is fit and displayed with classical iid SEs only, and the 'Statistical Significance' paragraph quotes t ≈ 5.3 with 'p-value well below 0.001' — numbers matching neither the displayed iid fit nor an HC1 refit.
- **ch08-F03** [methods/error] L358-359: Infant-mortality prose claims 'highly statistically significant (t ≈ -5.9, p < 0.001)' but the chapter's own displayed outputs give iid t=-2.33 (p=0.027) and HC1 robust t=-1.35 (p=0.188): under the robust SEs that Key Concept 8.2 directly below calls 'essential for valid inference', the relationship is NOT significant at the 5% level — the stated conclusion flips.
- **ch08-F04** [methods/error] L711-711: Section header '### CAPM Results with Robust Standard Errors' is empty — no robust fit exists anywhere in section 8.3 (the CAPM regression is iid-only), while the surrounding interpretation quotes a beta t-statistic of ~21.5 that matches neither the displayed iid fit (t=9.41) nor an HC1 refit (t=7.88).
- **ch08-F05** [methods/error] L886-886: Section header '### Okun's Law with Robust Standard Errors' is empty — the Okun regression is fit and displayed with iid SEs only, and the later hypothesis-test prose quotes 't-statistic of ~3.4' for testing beta=-2.0, matching neither the iid computation (t=2.35) nor an HC1 refit (t=2.52).
- **ch08-F27** [results/error] L358-359: Prose claims the infant-mortality relationship is 'highly statistically significant (t ~ -5.9, p < 0.001)', but executed iid output gives t = -2.33 (p = 0.027) and the robust (HC1) output displayed immediately after gives t = -1.35 (p = 0.188) — under the chapter's own robust-SE convention the significance conclusion FLIPS to insignificant at 5%.
- **ch08-F51** [results/drift] L1305-1305: Exercise 3(a) states Walmart beta = 0.45 and Target beta = 1.25 as facts about the chapter's stocks; betas computed from the chapter's dataset are 0.75 and 1.07.
- **ch08-F52** [interpretation/error] L1019-1054: The entire post-2008 actual-vs-predicted discussion has the direction reversed: after 2010 actual GDP growth is consistently BELOW the Okun prediction, not above it, so L1019 ('actual GDP exceeding predictions'), L1032-1038 ('consistently exceeds predicted growth'; 'GDP grew faster than unemployment changes suggested'), L1052 ('model slightly under-predicts the severity' of 2009), and L1054 ('weak predicted recovery contrasts with actual decent GDP growth') all state the opposite of what the fitted model and data show, and L1032 also misdescribes the actual-growth line as '(black line)' when it is solid cyan (#22d3ee).
- **ch08-F53** [interpretation/error] L358-359: The infant-mortality slope is described as 'highly statistically significant (t ≈ -5.9, p < 0.001), providing strong evidence', but the chapter's own executed outputs displayed immediately above give iid t = -2.33 (p = 0.027) and robust HC1 t = -1.35 (p = 0.188), so with the robust SEs that Key Concept 8.2 calls 'essential for valid inference' the coefficient is not significant at conventional levels — a stated-conclusion flip that a number swap cannot repair.
- **ch08-F70** [readability/drift] L711-711: Header '### CAPM Results with Robust Standard Errors' is completely empty — no prose and no code between it and the next header, so the book renders a section title that promises robust results and delivers nothing.
- **ch08-F71** [readability/drift] L886-886: Header '### Okun's Law with Robust Standard Errors' is completely empty — no prose and no code before the next header at L888.
- **ch08-F72** [readability/drift] L269-271: The '### Robust Standard Errors' subsection under 8.1 contains only one prose sentence saying robust SEs are standard, but no robust fit of the life-expectancy regression ever appears in the chapter body — the section promises output it never shows.
- **ch08-F93** [format/drift] L711-712: H3 heading '### CAPM Results with Robust Standard Errors' has zero body content — it is immediately followed by the next H3 at L713, producing an empty section in the rendered book.
- **ch08-F94** [format/drift] L886-887: H3 heading '### Okun's Law with Robust Standard Errors' has zero body content — immediately followed by the next H3 at L888, producing an empty section in the rendered book.

## ch09 (3 report items)

- **ch09-F10** [methods/error] L760: Key Takeaway 3 presents R² comparison as a model-selection criterion without the same-dependent-variable restriction.
- **ch09-F38** [code/style] L1091: Per the SE-convention policy, ch09 needs one labeling sentence at the first inference discussion noting that all fits use classical (iid) SEs as AED does before Chapter 12.
- **ch09-F60** [interpretation/error] L1089-1106: Task 2 guides students to interpret 100×β₁ as the percentage productivity gain per unit of human capital, but the estimated semi-elasticity is ≈1.18 — far outside the |β|<0.1 range where the approximation is valid (approximate 118% vs exact 100×(e^1.18−1) ≈ 225%), so the task teaches a badly misleading magnitude without invoking the exact formula from Key Concept 9.3.

## ch10 (8 report items)

- **ch10-F01** [methods/error] L592-612: The VIF demonstration computes uncentered VIFs (design matrix passed to statsmodels variance_inflation_factor without an intercept), so the executed output reports 'Warning: Maximum VIF = 57.82 indicates multicollinearity issues' — contradicting the chapter's own glossary claim at line 148 that 'the actual six predictors in the Davis dataset show only moderate VIFs' and misapplying the centered-VIF rule of thumb (VIF>5 moderate, VIF>10 severe) taught in Key Concept 10.8.
- **ch10-F02** [methods/error] L978-993: Exercise 5's VIF table (Size 40.1, Bedrooms 57.8, Bathrooms 34.7, Age 21.0) reproduces the miscomputed uncentered VIFs from the chapter's flawed code cell, teaching students that a house-price regression like the chapter's exhibits 'the most severe multicollinearity' when the correctly computed centered VIFs for these data are all below 2.
- **ch10-F22** [code/error] L536-537: pyfixest Feols objects have no _aic/_bic attributes, so the hasattr-guarded AIC/BIC columns in the section 10.7 comparison table (and cheat-sheet STEP 6 at L855-856, and the Task 5 hints at L1133, L1369, L1388-1389) always evaluate to NaN — the AIC/BIC model comparison the section and KC 10.7 promise never shows real values.
- **ch10-F31** [code/drift] L982-987: Exercise 5's hand-typed VIF table (Size 40.1, Bedrooms 57.8, Bathrooms 34.7, Age 21.0) reproduces the buggy uncentered VIFs from the chapter's no-constant computation; once the VIF code is fixed (correct centered values 1.69/1.73/1.30/1.18) these values no longer echo any chapter output.
- **ch10-F32** [code/style] L1025-1032: Case Study 1's loader is a plain ```python fence, so dat_2014 is never created in the book or the exported Colab notebook — unlike the ch09 pattern adopted by the campaign (loader as live {python} cell, task code as plain fences) and unlike Case Study 2 in this same chapter, which is fully live.
- **ch10-F36** [results/error] L148-149: Key Concept 'Perfect Collinearity' example claims the six Davis predictors 'show only moderate VIFs', directly contradicting the chapter's executed VIF output, which prints 'Warning: Maximum VIF = 57.82 indicates multicollinearity issues.'
- **ch10-F37** [interpretation/error] L148-148: Key Concepts example claims the six Davis predictors 'show only moderate VIFs' but the chapter's own VIF cell prints 'Warning: Maximum VIF = 57.82 indicates multicollinearity issues' because the code computes uncentered VIFs (no intercept added to X), so the KC, the section-10.8 output, KC 10.8's >10='severe' rule, and Exercise 5's table (40.1/57.8/34.7/21.0, reusing the same inflated values) give the student contradictory conclusions about the same regression.
- **ch10-F68** [format/style] L51-55: Chapter outline punctuation drifts from the (load-bearing) section headings: outline says '10.1 Example: House Price...' and '10.5 Partial Effects — The FWL Theorem' while the headings use '10.1 Example - House Price...' and '10.5 Partial Effects - The FWL Theorem' (colon vs hyphen, em dash vs hyphen).

## ch11 (25 report items)

- **ch11-F04** [methods/drift] L1692: Key Concept 11.10 mischaracterizes the joint F-test as confirming that the factors matter 'beyond what each contributes alone' — the Task 4 test (H₀: β₁ = β₂ = 0 vs intercept-only) establishes collective significance, not incremental contribution beyond individual effects.
- **ch11-F31** [code/error] L564-564: The H3 heading '### Using statsmodels t_test' credits statsmodels and a t_test method, but the cell uses pyfixest's wald_test and the lead-in sentence itself says 'Python's pyfixest package' — heading words are load-bearing per the campaign brief, so the rename (e.g. to a pyfixest wald_test title) needs owner coordination rather than a direct fix.
- **ch11-F32** [results/error] L676-700: The §11.5 overall F-test cell prints F=1.5069, p=0.222, 'Fail to reject H₀' — flatly contradicting the hand-typed prose (L607-624: F=6.83, p=0.0003, Reject H₀; also KC example L122 and L320), because `model_full._f_statistic` (L683) is not the classical overall F.
- **ch11-F33** [results/error] L571-572: The wald_test restriction matrix [[0,0,0,0,0,1,0]] selects the 6th coefficient (`age`), not `size`, so the executed statistic 1.5069 (p=0.2196) fails to confirm the manual test it is meant to replicate (t=1.19, p=0.245 at L478-482), and this same call overwrites `_f_statistic` to 1.5069 for all downstream cells.
- **ch11-F34** [results/error] L828-838: The §11.6 manual F-statistic cell prints 'From model output: 1.5069' and 'Match: False' twice — the chapter's own verification visibly fails in the rendered output even though the manual calculation (6.8261) is correct.
- **ch11-F35** [results/error] L1197-1219: The F-distribution figure plots the observed F at 1.5069 (inside the acceptance region, left of the 2.55 critical line) while its printed caption asserts 'The observed F-statistic (1.51) exceeds the critical value (2.55). This leads us to reject the null hypothesis.' — false as printed and self-contradictory.
- **ch11-F36** [results/error] L1066-1067: The §11.7 model-comparison table prints F-stats 1707.2666 / 1097.6882 / 1.5069 (p-values 0.000000 / 0.000000 / 0.222145), contradicting the hand-typed prose at L858-871 which gives the correct classical values 43.58 / 21.03 / 6.83.
- **ch11-F37** [results/error] L1384-1385: Cheat-sheet STEP 5 prints `model_full._f_statistic` — a fresh self-contained run outputs 'Overall F-test: F = 435.4036, p = 1.110223e-16', not the chapter's classical F = 6.83 (p = 0.0003).
- **ch11-F38** [code/error] L1572-1574: Case Study 1 code references Mendez dataset columns `rk` and `hc` that do not exist (actual columns: `kl`, `h`), so the loader crashes with KeyError and none of CS1's code runs as printed (cross-lens with Lens B; verified by recomputation).
- **ch11-F39** [results/error] L1645-1646: Case Study 1 Task 4 prints `model_prod._f_statistic` as the 'Overall F-statistic' — once the column-name crash is repaired, this prints 39204.25 while the correct classical overall F is 663.43, contradicting the manual RSS-based F-test printed directly below it.
- **ch11-F46** [interpretation/error] L614-618: The 'Interpreting the Overall F-test' section asserts F = 6.83, p = 0.0003, 'Decision: Reject H0 - The model is highly statistically significant', but the executed code cell it interprets prints F-statistic 1.5069, p = 0.222, 'Result: Fail to reject H0' — the stated conclusion is flipped relative to the output readers actually see.
- **ch11-F47** [interpretation/error] L1218-1219: The F-distribution figure cell unconditionally prints 'The observed F-statistic (...) exceeds the critical value (...)' and 'This leads us to reject the null hypothesis', but as executed it renders 'The observed F-statistic (1.51) exceeds the critical value (2.55)' — a false statement (1.51 < 2.55) followed by an invalid rejection conclusion.
- **ch11-F49** [interpretation/error] L1692: Key Concept 11.10 mischaracterizes what an F-test shows: a joint F-test confirms joint significance, not that 'both factors jointly matter beyond what each contributes alone' — incremental contribution is established by subset F-tests, a distinction the chapter itself teaches.
- **ch11-F53** [interpretation/style] L412: Causal verb ('adds at least $36 to the price') in the practical-meaning discussion of the CI lower bound.
- **ch11-F59** [readability/style] L398-414: Section '### Interpreting the Confidence Intervals' appears BEFORE the code cell (L416-423) that produces the confidence-interval output it interprets; move it after that cell (or after the manual-CI cell at L429-448).
- **ch11-F60** [readability/style] L472-488: Section '### Interpreting the Hypothesis Test Result' (t=1.19, p=0.245 for H0: beta_size=50) sits at the end of section 11.3, before section 11.4 even introduces the test; move it after the test cell at L515-536.
- **ch11-F61** [readability/style] L603-650: Sections '### Interpreting the Overall F-test' and '### Interpreting the Joint Test of Subset Variables' appear before the code cells that produce those results (overall F-test cell L676-700, subset-test cell L706-732); move each after its cell.
- **ch11-F62** [readability/style] L652-674: Section '### Interpreting the Sum of Squares Decomposition' appears in section 11.5, before the sum-of-squares code cell it interprets, which lives in section 11.6 (L797-839); move it after that cell.
- **ch11-F63** [readability/style] L738-765: Section '### Interpreting the Subset F-test' quotes RSS values that are only printed by the cell at L961-991 in section 11.6, yet it appears in section 11.5 before the 11.6 heading; move it directly after that cell.
- **ch11-F64** [readability/style] L849-881: Section '### Interpreting the Model Comparison' (three-model R2/adjusted-R2 comparison) appears under section 11.6's 'Subset F-test' heading, before the restricted model is estimated (L883) and before the three-model comparison code in section 11.7 (L1039-1075); move it after the 11.7 comparison cell.
- **ch11-F65** [readability/style] L895-923: Section '### Interpreting Coefficient Stability Across Models' appears before the coefficient-comparison code cell (L1081-1103, section 11.7) that produces the table it interprets; move it after that cell.
- **ch11-F66** [readability/style] L925-959: Orphaned heading '### Manual F-test Calculation' (L925) is immediately followed by another H3 with no content, its code cell sitting at L961-991 after an unrelated section; and '### Interpreting Robust Standard Errors' (L927-959) precedes the robust-SE code cell at L1122-1142 that produces the numbers it discusses; reattach the heading to its cell and move the robust-SE interpretation after L1122-1142.
- **ch11-F67** [readability/style] L260-264: Comment-only code cell for section 11.1 contains no executable code and renders as an empty-output cell in the book and Colab; consider deleting it (the surrounding markdown already carries the content).
- **ch11-F68** [readability/style] L361-371: Section '### Economic Interpretation' (L361-371) largely repeats the earlier '### Interpreting the Regression Results' (L305-326) — same size coefficient, same significance verdicts, same multicollinearity point; consider merging or trimming one of the two.
- **ch11-F99** [format/style] L925-927: '### Manual F-test Calculation' (L925) is an empty section — it is immediately followed by another H3 ('### Interpreting Robust Standard Errors', L927) with no content between them; the manual F-test code it presumably introduces sits at L961-991 under a different heading sequence.

## ch12 (29 report items)

- **ch12-F21** [code/error] L1594-1598: Task 4 code cannot run: it calls pred.summary_frame(alpha=0.05) on a variable `pred` that is never defined, and summary_frame/mean_ci_lower/obs_ci_lower are statsmodels PredictionResults API that pyfixest predictions (plain numpy arrays) do not have.
- **ch12-F26** [code/error] L1804-1806: CS2 Task 3 guided template directs students to pred.summary_frame(alpha=0.05) with mean_ci_lower/mean_ci_upper — an undefined variable plus statsmodels-only API unavailable on pyfixest predictions, so the instructed 95% CI for the conditional mean cannot be produced by following the template.
- **ch12-F27** [code/error] L1823-1861: CS2 Task 4 is internally contradictory and unrunnable as templated: instruction 1 says to compute the PI manually, instruction 2 says to report it via obs_ci_lower/obs_ci_upper, and the example code again calls the undefined pred.summary_frame — none of which exists in pyfixest.
- **ch12-F28** [code/error] L291-325: Section 12.2 announces 'Let's systematically compare the standard errors' and the following interpretation block repeatedly references an 'SE Ratio column', but no code cell produces any default-vs-robust comparison table.
- **ch12-F29** [code/error] L396-438: The 'Interpreting HAC Standard Errors' block interprets a three-way comparison (default SE, HAC lag 0, HAC lag 5) for mean GDP growth that no chapter cell ever computes.
- **ch12-F30** [code/error] L491-662: Section 12.3 prose interprets outputs that no cell produces: a two-panel CI-vs-PI figure ('Looking at the two panels', red/blue), a 2000-sq-ft prediction with specific intervals, an announced 'Prediction at Specific Values' computation, and an announced 'Manual Calculation of Standard Errors' — the only code in this stretch is the simple-regression fit (dump cell 8).
- **ch12-F31** [code/error] L943-1162: 'Interpreting Figure 12.3' (L945) analyzes a power curve, and 'Illustration: Power of a Test' (L1160-1162) says 'Let's visualize how test power depends on the true effect size' — but no power-curve cell exists anywhere in the chapter body; Figure 12.3 is never generated.
- **ch12-F49** [results/error] L365-369: Executed autocorrelation cell prints NaN at every lag (growth has 4 missing values), contradicting the code comment 'Positive lag 1 correlation suggests persistence' and KC callout L91's claim that 'the chapter computes a positive lag-1 autocorrelation'; the correlogram cell (L372-384) is fed the same NaN-containing series.
- **ch12-F50** [results/error] L291-299: Section 'Comparison: Default vs. Robust Standard Errors' promises a systematic comparison and the following interpretation says 'Looking at the SE Ratio column', but no cell in the chapter produces any SE-ratio table — readers are asked to interpret output that does not exist.
- **ch12-F51** [results/error] L396-415: Section 'Interpreting HAC Standard Errors' interprets 'the three standard error estimates for the mean growth rate' (Default, HAC lag 0, HAC lag 5), but no cell in the chapter computes any HAC standard error, and the cheat-sheet version uses lag 4, not lag 5.
- **ch12-F52** [results/error] L495-547: Prose interprets a CI-vs-PI figure that is never produced: 'Looking at the two panels... prediction interval (blue)... confidence interval (red)' (L495) and 'This figure illustrates...' under 'Visualization: Confidence vs. Prediction Intervals' (L542-547), but no plotting cell exists in section 12.3.
- **ch12-F53** [results/error] L656-662: Headings 'Prediction at Specific Values' ('Let's predict house price for a 2000 square foot house.') and 'Manual Calculation of Standard Errors' ('Let's manually calculate the standard errors...') are followed by no code cells — the promised computations, whose numbers the surrounding prose quotes, do not exist in the chapter.
- **ch12-F54** [results/error] L943-947: 'Reading the Power Curve' interprets 'Figure 12.3' and section 12.7's 'Illustration: Power of a Test' (L1160-1162) says 'Let's visualize how test power depends on the true effect size', but no power-curve cell exists in the chapter — Figure 12.3 is never generated (and no Figure 12.2 exists either).
- **ch12-F55** [interpretation/error] L91-91: The Key Concepts 'Autocorrelation' example asserts 'the chapter computes a positive lag-1 autocorrelation' and that the Newey-West HAC SE around mean growth is larger than the default SE, but the executed ACF output is NaN at every lag and no HAC SE is ever computed in the chapter body.
- **ch12-F56** [interpretation/error] L291-325: Section 12.2's 'Interpreting the Comparison: What Changed?' tells readers 'Looking at the SE Ratio column, we can see how robust standard errors differ from default ones' (L299), but no SE-ratio table or comparison code cell exists anywhere in the chapter body.
- **ch12-F57** [interpretation/error] L396-438: 'Interpreting HAC Standard Errors' interprets 'the three standard error estimates for the mean growth rate' (default, HAC lag 0, HAC lag 5) that are never computed in the chapter body, so readers are asked to compare numbers that do not exist.
- **ch12-F58** [interpretation/error] L491-662: Section 12.3 contains four interpretation blocks that reference figures and computations absent from the chapter: 'Looking at the two panels' (L495) with color-coded CI/PI bands, 'This figure illustrates...' (L542-547), 'Looking at our predictions' for a 2000 sq ft house (L549-596), and 'The manual calculations reveal...' (L598-654) — while the two headers that announce those computations ('Prediction at Specific Values' L656-658 and 'Manual Calculation of Standard Errors' L660-662) are stubs with no code at all.
- **ch12-F60** [interpretation/error] L704-704: The printed conclusion 'Multiple regression provides more precise predictions.' is contradicted by the chapter's own executed numbers: the six-regressor model's RMSE and approximate prediction interval are both LARGER than the simple size-only model's.
- **ch12-F61** [interpretation/error] L943-1041: 'Reading the Power Curve' interprets 'Figure 12.3' which is never produced anywhere in the chapter (the announcing stub '### Illustration: Power of a Test' at L1160-1162 in section 12.7 has no code), it sits two sections before that stub inside 12.5, and its numeric thresholds are wrong for the stated parameters (n=30, SE=15, alpha=0.05).
- **ch12-F64** [readability/error] L291-325: The 'Comparison: Default vs. Robust Standard Errors' lead-in ('Let's systematically compare the standard errors...') is immediately followed by its interpretation section, which walks the reader through 'the SE Ratio column' of a comparison table that is never produced anywhere in the chapter body — the comparison code cell is missing.
- **ch12-F65** [readability/error] L396-438: The 'Interpreting HAC Standard Errors' section interprets 'three standard error estimates for the mean growth rate' (default, HAC lag 0, HAC lag 5 / Newey-West) that are never computed in the chapter — the chapter body only computes the ACF and plots the correlogram, so readers are told what nonexistent results 'tell us'.
- **ch12-F66** [readability/error] L491-596: Three consecutive prediction sections interpret a figure and results that do not exist: 'Looking at the two panels, you'll notice the prediction interval (blue) is dramatically wider than the confidence interval (red)' (L495) refers to a two-panel figure never plotted; '### Visualization: Confidence vs. Prediction Intervals / This figure illustrates...' (L542-547) is followed by no figure; and 'Looking at our predictions...' (L551+) discusses concrete CI/PI numbers for a 2000 sq ft house before any prediction code has run.
- **ch12-F67** [readability/error] L656-662: Two orphaned lead-ins promise code that never follows: '### Prediction at Specific Values / Let's predict house price for a 2000 square foot house.' and '### Manual Calculation of Standard Errors / Let's manually calculate the standard errors to understand the formulas.' — the next content is the '### Prediction with Multiple Regression' heading; L602 ('The manual calculations reveal...') also references these missing calculations.
- **ch12-F68** [readability/error] L943-1041: 'Reading the Power Curve: What It Tells Us' interprets 'Figure 12.3' feature-by-feature (gray line, green dashed 0.80 threshold, n=30, SE=15), but Figure 12.3 is never produced anywhere in the chapter — the power-curve code exists only in the non-runnable cheat sheet — and the section sits under 12.5 Best Estimation Methods while the power topic belongs to 12.7 Best Tests.
- **ch12-F69** [readability/error] L1160-1162: '### Illustration: Power of a Test / Let's visualize how test power depends on the true effect size.' is followed by no code at all — the next content is Key Concept 12.8 — leaving the promised visualization (and the natural home for the 'Interpreting Figure 12.3' section at L943) missing from section 12.7.
- **ch12-F74** [readability/error] L1043-1067: The section 12.5 code cell prints only bare outline headers ('1. Gauss-Markov Theorem:', '2. When assumptions fail:', ...) while all of the actual content sits in code comments, so the rendered output is an empty numbered outline that tells the reader nothing.
- **ch12-F75** [readability/style] L833-837: The section 12.4 code cell is a no-op — a banner plus one comment, producing zero output — which reads as a broken or forgotten cell in Colab and the book.
- **ch12-F76** [readability/style] L765-831: The long 'Bootstrap Confidence Intervals: An Alternative Approach' block sits under section 12.4 Nonrepresentative Samples, though bootstrap belongs with 12.6 Best Confidence Intervals, where the same material is summarized again (L1089-1095 and Key Concept 12.7 at L1109-1111).
- **ch12-F77** [readability/style] L843-914: 'The Type I vs. Type II Error Tradeoff' block (including the 2x2 decision table) sits under section 12.4 Nonrepresentative Samples but belongs to 12.7 Best Tests, which repeats the identical 2x2 table and much of the same content at L1115-1158.

## ch13 (6 report items)

- **ch13-F03** [methods/error] L682-684: Key Concept 13.5 states the OVB equals the omitted variable's coefficient 'times its correlation with the included regressor', but the bias term is β₃×γ where γ is the auxiliary-regression SLOPE, not a correlation.
- **ch13-F04** [methods/style] L449-472: The CRS test is a classical RSS-based F-test that assumes iid errors, immediately after the chapter declares default iid SEs 'WRONG here!' and mandates HAC for this time series — an internal methods inconsistency left unacknowledged.
- **ch13-F12** [code/error] L457-472: The constant-returns-to-scale test is a classical iid RSS-based F-test (executed p = 0.663) even though the model is estimated with HAC (NW) SEs and the section explicitly says default SEs are WRONG here; the prose's hand-typed p = 0.636 (L95, L476, L1678) exactly matches the HAC-robust Wald test the code should arguably run instead.
- **ch13-F43** [interpretation/error] L600-604: The post-1970 Phillips 'sign reversal' (and the augmented model's 'return' of the negative slope) is presented as a substantive result, but both urate coefficients are statistically insignificant — significance is never discussed.
- **ch13-F44** [readability/style] L972-1020: The RAND RCT code cell is ~47 lines and bundles three independently interpretable outputs (data load + mean-spending-by-plan table, the fit_rct.summary() regression, and the joint F-test) with no intervening prose.
- **ch13-F45** [readability/style] L1459-1516: The Institutions/IV code cell is ~56 lines and bundles four independently interpretable outputs (data load + summary, biased OLS, first-stage F, and 2SLS + OLS-vs-IV comparison) with no intervening prose.

## ch14 (13 report items)

- **ch14-F01** [methods/error] L495-498: The model-evolution narrative claims adding education 'reduces the gender coefficient by ~$6,000' to a conditional gap of approximately -$10,000, but execution shows Model 2's education-adjusted gender coefficient is -$18,258 (larger magnitude than the -$16,396 unconditional gap), so the 'controls shrink the gap' / 'education explains part of the raw gap' OVB-mediation reasoning is directionally reversed.
- **ch14-F02** [methods/error] L135-136: The 'Conditional vs. Unconditional Gap' Key Concept example claims adding education shrinks the conditional gender gap to roughly -$10,000, but the education-adjusted gender coefficient is actually -$18,258 (larger in magnitude than the -$16,396 unconditional gap), so the direction of the compositional/OVB claim is wrong.
- **ch14-F08** [code/error] L1509-1518: Task 5 commented scaffold uses statsmodels `anova_lm` (never imported, does not work on pyfixest fits) and references undefined variables `model_base`/`model_dep_only`/`model_full` while the fits are named `fit_base`/`fit_dep_only`/`fit_full`.
- **ch14-F13** [results/error] L136: KC 'Conditional vs Unconditional Gap' claims education shrinks the gap to ~-$10,000 and age+hours to -$5,000/-$8,000, but the executed conditional gaps contradict this (gap grows with education; no model yields -$5k to -$8k).
- **ch14-F14** [results/error] L495-498: 'Model Evolution' says Model 2 (+Education) shrinks the gap to ~-$10,000 and reduces the gender coefficient by ~$6,000, but executed Model 2 gender coefficient is -18,258 (magnitude increases by ~$1,900).
- **ch14-F15** [results/error] L518-519: 'Key Insights' asserts the gap decreases substantially from -$16,000 to approximately -$5,000/-$8,000 with controls; no executed specification supports a conditional gap of -$5k to -$8k.
- **ch14-F16** [results/error] L804-812: The worker-type 'Example Interpretation' block reports coefficient signs opposite to the executed regressions (self-employed shown earning less than private and government more, when the data show the reverse).
- **ch14-F17** [results/drift] L968: Key Takeaways cites t = -4.71 for the gender coefficient, which is the classical/default-SE t; the chapter's headline Model 1 uses HC1 robust SEs, whose t is -5.10.
- **ch14-F18** [results/drift] L570: Section 14.3 characterizes the Gender×Education interaction as 'around -$1,000 to -$2,000', but the executed interaction coefficients are about -$2,100 to -$2,800.
- **ch14-F19** [interpretation/error] L495-498: The 'Model 2 (+ Education)' interpretation claims the gender gap shrinks to about -$10,000 when education is controlled, but the executed Model 2 gender coefficient is -18,258 — controlling for education makes the gap LARGER, not smaller.
- **ch14-F20** [interpretation/error] L518-519: The 'Key Insights' summary asserts the gender gap decreases substantially from -$16,000 to -$5,000/-$8,000 after controls, a conditional-gap magnitude that no fitted model produces.
- **ch14-F21** [interpretation/error] L136-136: The 'Conditional vs. Unconditional Gap' Key Concept example repeats the false claim that adding education shrinks the conditional gap to ~-$10,000 (and further to -$5k/-$8k), contradicting the executed Model 2 coefficient of -18,258.
- **ch14-F22** [interpretation/error] L804-805: The worker-type 'Example Interpretation' inverts the actual earnings ranking — it portrays self-employed as earning ~$5,000 LESS than private, but the chapter's own regression shows self-employed earn ~$17,000 MORE and are the highest-earning group.

## ch15 (14 report items)

- **ch15-F01** [methods/error] L101-106: The MEM Key Concept panel wrongly claims the marginal-effect-at-the-mean differs from the AME for the quadratic earnings-on-age model; for a quadratic the two are identical.
- **ch15-F02** [methods/error] L916-921: The interaction section attributes the conclusion 'education's effect is age-dependent' to a joint F-test that only establishes whether education matters at all, not whether the interaction is significant.
- **ch15-F15** [results/error] L106-106: KC 'MEM' example claims a marginal effect of roughly +$1,000 to +$1,500/yr for the average worker and that MEM differs from AME, but executed MEM = $536 and for a quadratic (ME linear in age) MEM equals AME exactly.
- **ch15-F16** [results/error] L121-121: KC 'MER' attributes ME_education of +$2,500/+$7,000/+$11,500 and a '4–5× ramp' to the chapter's interaction model, but the executed model gives $5,241/$5,677/$6,112 — a flat 1.17× ramp from a statistically insignificant interaction.
- **ch15-F17** [results/error] L151-151: KC 'Standardised Variable' example claims education and lnhours are the largest standardised effects, but executed β* are largest for age (0.68) and agesq (−0.57).
- **ch15-F22** [results/error] L742-755: Standardized-coefficient narrative ranks Education as the strongest predictor with age β*≈0.15–0.20, but executed β* make age (0.68) and agesq (−0.57) by far the largest, with education third (0.30).
- **ch15-F23** [results/error] L883-902: Interaction-model narrative uses stale coefficients (education −$10,000 to −$5,000 'often negative', interaction +$200 to +$400) and derives ME +$2,500/+$7,000/+$11,500 with a '4–5× larger' payoff, all contradicted by the executed positive-and-flat fit.
- **ch15-F27** [results/drift] L335-336: Levels-model age effect is described as ≈$800–$1,200 (~$1,000) per year here and '+$1,000/year' at L527, but the executed levels age coefficient is 525.
- **ch15-F28** [results/drift] L347-348: Log-linear age coefficient is described as ≈0.01–0.02 (1–2%), but the executed semi-elasticity is 0.0078 (~0.8%/yr), which the code prints as coef 0.008.
- **ch15-F29** [interpretation/error] L883-902: The entire §15.5 narrative claims returns to education rise 4-5× with age, driven by a negative education main effect and a large positive interaction — but the fitted model shows the opposite and the interaction is statistically insignificant.
- **ch15-F30** [interpretation/error] L742-769: The standardized-coefficient section concludes “Education dominates / strongest predictor” and ranks age as only “moderate” (β*≈0.15-0.20), contradicting the section’s own printed output where age has by far the largest standardized coefficient.
- **ch15-F31** [interpretation/error] L105-106: The MEM Key Concept example claims the marginal effect at the mean differs from the AME for the quadratic earnings model, but for a quadratic the marginal effect is linear in age, so AME and MEM are mathematically identical here.
- **ch15-F32** [interpretation/error] L121-121: The MER Key Concept glossary example presents a “4–5× ramp in the return to schooling across the working life” as established fact, but the underlying interaction is insignificant and the real ramp is ~1.2×.
- **ch15-F33** [interpretation/error] L151-151: The Standardised-Variable glossary example asserts that education and lnhours are the largest standardised effects, but age and age² have the largest standardised coefficients in that model.

## ch16 (8 report items)

- **ch16-F02** [methods/error] L69-69: The Endogeneity Key Concept example states the chapter regresses "economic growth on democracy" with democracy as the endogenous regressor, but the chapter's actual specification is `democracy ~ growth` (democracy is the dependent variable, growth the regressor), so the illustrative example describes a different model than the one estimated.
- **ch16-F07** [code/error] L1788: Case Study 1 dataset bullet documents variables rk/hc/rgdppc/tfp that do not exist in the Mendez dat.csv (actual: kl/h/GDPpc/TFP); descriptions ('physical capital' for kl) need author revision.
- **ch16-F08** [code/drift] L2074-2096: Case Study 2 Task 4 example scaffold defines a pyfixest fit then calls OLSInfluence(model)/model.params — 'model' is undefined and OLSInfluence requires a statsmodels OLS result, not a pyfixest object; if uncommented it errors and contradicts the chapter's own correct smf.ols pattern (CELL 18, L1279).
- **ch16-F15** [results/drift] L325: Intercept VIF stated as ≈10-15 but statsmodels reports 411.3 for the const row (contradicts executed output; note intercept VIF is not econometrically meaningful).
- **ch16-F22** [results/error] L757-787: Autocorrelation narrative claims residual ρ₁≈0.95-0.98, ρ₅≈0.85-0.90, ρ₁₀≈0.75-0.85 and a variance-inflation factor of 39, but the chapter's actual AR(1) simulation (ρ=0.8) produces lag-1 ACF 0.80, lag-5 0.31, lag-10 0.09 and factor (1+0.8)/(1-0.8)=9 — the whole 'interest rate / levels regression' essay describes a scenario the chapter never runs.
- **ch16-F23** [results/drift] L568: Section asserts robust SEs are '20-40% larger' than standard, but executed ratios are 0.98x (age) and 1.13x (education) — at most 13% larger, and smaller for age.
- **ch16-F26** [interpretation/error] L749-879: The entire 16.6 discussion block interprets an empirical 'interest rate' analysis that the chapter never performs -- the code only simulates generic AR(1) data.
- **ch16-F28** [interpretation/drift] L556-556: The section concludes the SE comparison 'reveals heteroskedasticity', but the actual robust/standard SE ratios are near 1.0, providing little evidence for it.

## ch17 (18 report items)

- **ch17-F01** [methods/error] L528-585: The pooled-OLS narrative claims cluster-robust SEs render the wins coefficient insignificant ('Complete reversal of inference'), but the executed cluster-robust fit is highly significant — the stated significance conclusion is inverted.
- **ch17-F02** [methods/error] L780-798: The fixed-effects narrative calls the wins coefficient 'Surprisingly not significant' (t≈1.25, p≈0.21) and 'imprecisely estimated,' but the executed FE fit is highly significant — the conclusion is inverted.
- **ch17-F04** [methods/error] L570-570: The two-way clustering formula is stated as standard errors adding linearly and subtracts a 'pooled' term, which is incorrect: variance matrices combine, not SEs, and the subtracted term is the team-by-time intersection, not the classical/pooled variance.
- **ch17-F08** [code/style] L886-891: In the logit cell, `model_logit.summary()` (L886) and `marginal_effects.summary()` (L891) are bare mid-cell expressions that are computed but never displayed, so the full logit table and the marginal-effects table the chapter relies on never appear in the book or Colab.
- **ch17-F14** [results/error] L445-447: The 'Typical Results' SE table (default 0.0030 / robust 0.0035 / cluster 0.0065; ratios 1.00/1.17/2.17x) does not sync with the executed pooled regression it annotates.
- **ch17-F15** [results/error] L529-540: Section 17.2 claims clustering flips the wins coefficient from significant to non-significant ('Complete reversal of inference!'), but the executed regression is significant with cluster SEs.
- **ch17-F16** [results/error] L574-585: 'The NBA Example' states the cluster-SE wins coefficient is 0.0055 (SE 0.0040), t=1.38, p=0.17, 'not statistically significant' — all contradicted by the live output.
- **ch17-F17** [results/error] L655-656: Section 17.3 Pooled-vs-FE 'Typical Results' table has wrong coefficients, SEs, and a grossly wrong FE within R² (0.65 vs 0.185).
- **ch17-F24** [results/error] L780-798: Section 17.3 claims the FE wins coefficient is 'surprisingly not significant' (t~1.25, p~0.21), but the executed FE regression is highly significant.
- **ch17-F25** [results/error] L115-115: KC AR(p) example claims a negative own-lag coefficient (~ -0.20) implying mean reversion in changes; the actual coefficient is positive.
- **ch17-F26** [results/error] L130-130: KC ADL example hand-types gamma coefficients (0.50/0.20/0.08) and a 0.78 long-run multiplier; the fitted ADL gives 0.74/-0.26/0.05 and a 0.53 multiplier.
- **ch17-F27** [results/error] L1121-1130: Section 17.6 ADL(2,2) coefficient ranges have two sign errors and out-of-range magnitudes versus the fitted model.
- **ch17-F28** [results/error] L1195-1197: 'Comparing Models' table R2 values conflict with the fitted models — the static R2 (0.25) even contradicts the chapter's own executed changes regression (0.571).
- **ch17-F30** [interpretation/error] L530-540: The 'Complete reversal of inference!' claim is false: cluster-robust SEs leave the wins coefficient highly significant, not insignificant.
- **ch17-F31** [interpretation/error] L582-585: The §17.2 'NBA Example' interpretation states the wins-revenue relationship is not significant and that prior significance was an artifact, both contradicted by the executed cluster-robust result.
- **ch17-F32** [interpretation/error] L780-792: The Fixed Effects 'Statistical Significance' block concludes the within-team wins effect is 'Surprisingly not significant' (t≈1.25, p≈0.21); the executed FE result is highly significant (t=5.35, p<0.001).
- **ch17-F33** [interpretation/error] L797-798: The 'Practical Implications' bullets describe the FE wins effect as 'imprecisely estimated' and needing a longer panel; the executed FE effect is precisely estimated and significant.
- **ch17-F47** [format/style] L1559-1559: Key Takeaways closes with a single-sentence '**Next steps:**' line rather than the ch01-style bold '**Next Steps:**' block of 3-5 bullets called for by the format table.
