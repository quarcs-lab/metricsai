# 2026-07-08 19:34 — Web-apps audit & improvement campaign (all 17 chapters)

Branch: `worktree-webapps-check`. A full statistical-correctness + readability + format-consistency
audit of every chapter web app (`web-apps/ch01`–`ch17`), delivered as **17 per-chapter commits**
(`e03fc25` … `ch03`). Chapter `.qmd` files were treated as the source of truth and **never edited**;
errors found in the chapters themselves are catalogued below for follow-up.

## Current state

- **17/17 web apps audited, fixed, committed.** Every dashboard rebuilt from its `template.html`.
- **All gates green fleet-wide:** `verify_app.py` 17/17 · `i18n_check.py` ALL CLEAN (EN/ES/JA parity)
  · `smoke_i18n.py` PASS · JS-math harness ALL PASS vs scipy/statsmodels · data-island audit 0 mismatches.
- Every prose change was made in all three languages (EN/ES/JA) in lockstep; template inline
  fallbacks synced to the string tables (a drift class no pre-existing gate caught).

## How it was run

1. **Phase 0 — verification harness** (session-only, in `SCRATCH/harness/`): extract each app's
   hand-rolled JS math (`tCDF`, `betaInc`, `tQuantile`, `normalCDF`, `olsFit`, `olsMultiple`,
   moments) and diff against scipy/statsmodels; recompute every embedded data-island stat;
   parse/execute the Python cheat-sheets; a template-fallback↔strings-`en` drift checker; and a
   parametrized per-chapter Playwright gate (the shipped `smoke_i18n.py` only covers ch04/10/16).
2. **Pilot ch04** end-to-end → user sign-off on the heading style + scope.
3. **Waves 1–5** (per-chapter: parallel auditor → adversarial verifier → apply → gates → commit).
   The adversarial verify pass repeatedly refuted over-flagged "prose improvements," preventing
   churn on already-correct text.

## Real bugs fixed (app was wrong)

| Chapter | Bug | Fix |
|---|---|---|
| ch11 | Python cheat-sheet **SyntaxError** (`²` backslash in an f-string) + a joint-F-test that used `wald_test().stat` (wrong for this pyfixest) and returned F=774, not the chapter's 6.83 | Converted the Python block's `\uXXXX` escapes to real chars; rewrote STEP 5 as a manual restricted/unrestricted F-test — now runs and prints F=6.83 (p=3.4e-4), 0.42 (p=0.83) |
| ch12 | `normalCDF` materially inaccurate (Φ(1.96)=0.981 vs 0.975) — made the Type I/II **power curve dip below its own α line**; `tQuantile` (Newton) diverged | Ported the harness-verified Zelen–Severo `normalCDF` + bisection `tQuantile`; live "Power at β=SE" 0.140→0.170, MDE 7.5→8.1 |
| ch16 | Autocorrelation widget's render used `t` as an AR(1) loop counter, shadowing the i18n `t()` → `TypeError: t is not a function` on load, blanking its charts | Renamed the loop counter to `ti`; all 8 charts render |
| ch16 | OVB widget's motivation had the **causal direction inverted** (asked "does democracy cause growth?" for a democracy~growth model) | Reframed to growth→democracy; relabeled a mislabeled "SE (robust)" stat and an over-claimed diagnostic-plot assumption |
| ch15 | Log-transform try-this told readers "log fits better (R² 0.176 vs 0.103)" — an invalid cross-DV R² comparison the widget's own Key Concept forbids | Reworded; prefer log for skew, not R² |
| ch17 | Two prose numbers contradicted the widgets' own output: lag-10 ACF ">0.80" (actual 0.62), robust-SE ratio "1.15×" (actual 0.97×) | Corrected both (all languages) |
| ch01 | Summary-stats callout printed the Bedrooms mean−median as a raw unrounded float with the sign dropped | Rounded + signed |
| ch06 | Stata cheat-sheet had an unterminated `display "===` string (broke STEP 7) | Closed the quote |
| ch01/02/15 | `skewness`/`kurtosisExcess` used a mixed-moment formula (sample SD but ÷n) matching neither convention | Replaced with population g1/g2 — now matches scipy exactly (what the chapters compute) |
| ch10/13 | R/Stata cheat-sheet HTML-escaping defects (double-escaped `&amp;gt;`, raw `<`) | Fixed to render correctly |
| ch01/05 | R cheat-sheets called `r2(model)` (returns a vector in fixest) | `r2(model, "r2")` |

## Format/consistency

- **All Title-Case widget headings normalized** to the ch02 sentence-case + em-dash-hook standard
  (ch04, 06, 07, 08, 09, 11, 12, 13(2), 14, 15, 16 — the sentence-case chapters were left as-is).
- Chapter-title wording aligned to the `.qmd`: ch12 "Additional→Further Topics…", ch16 "Model and
  Data Verification→Checking the Model and Data". ch17's title left as the app's cleaner wording
  (flagged, semantically identical).
- **Badge "anomaly" was a false alarm** — the flagged decision-badges are legitimate REJECT/FAIL
  test indicators; not touched.

## Deferred (approved as a separate follow-on): widget activations

The survey's "activate static widgets" + "expand ch01/ch03" workstream turned out to be major
feature engineering (≈13 static-widget activations + 4 new widgets, each needing `build.py`
precompute + new JS + trilingual prose). Per the user's decision, the **correctness/readability/
format audit was completed first** across all 17; the activations remain a dedicated next pass.
Design specs already exist (per-widget interaction + build.py additions) from the planning phase.

## `.qmd` errors flagged (chapter is wrong; app is correct) — 19 items, NOT edited

In several chapters the **web app is the correct source** and the chapter prose is stale/incorrect
(notably **ch14 Q15**: the chapter says adding education *shrinks* the gender gap to ≈−$10k, but the
regression shows it *widens* to −$18,258 — a suppressor effect; repeated in 3 places).

| ID | Ch | Issue (short) |
|----|----|----|
| Q1 | ch07 | causal "increases/adds $73.77" vs its own code's "associated with" |
| Q2 | ch11 | Model-2 coef $73.65/SE $11.50 stale (correct $72.41/$13.30) |
| Q3 | ch12 | §12.3 prediction RMSE ~4× too big |
| Q4 | ch12 | §12.5 power-vs-effect figures internally loose |
| Q5 | ch06 | MC section β₀/β₁ vs chapter's β₁/β₂ convention |
| Q6 | ch08 | CAPM Coca-Cola t/R²/n internally inconsistent |
| Q7 | ch08 | infant-mortality coef/t/R² stale |
| Q8 | ch08 | outlier table "+33%" should be "+41%" |
| Q9 | ch08 | causal "improves/reduces" on observational data |
| Q10 | ch13 | CRS F-test classical vs the section's stated HAC method |
| Q11 | ch17 | pooled/FE/within-R² results table stale |
| Q12 | ch17 | panel dims + between/within SD disagree with data |
| Q13 | ch17 | levels spurious-reg spec (trend vs no-trend) differs |
| Q14 | ch17 | title wording (app cleaner; optional) |
| **Q15** | **ch14** | **gender gap: chapter says shrinks to −$10k; actually widens to −$18,258** |
| Q16 | ch15 | §15.6 retransformation figures don't match dataset |
| Q17 | ch01 | narrative intercept 115,952 stale (correct 115,017) |
| Q18 | ch01 | worked prediction $264,012 stale (correct $262,559) |
| Q19 | ch03 | two inconsistent coin-toss SDs reported |

## Verification (how to re-check)

```bash
VENV=/Users/carlosmendez/Documents/GitHub/metricsai/.venv/bin/python3
for c in $(seq -w 1 17); do $VENV .claude/skills/web-app/scripts/verify_app.py web-apps/ch$c/dashboard.html; done
$VENV scripts/i18n_check.py     # ALL CLEAN
$VENV scripts/smoke_i18n.py     # ALL PHASE 5 TESTS PASSED
```

The session harness (`SCRATCH/harness/`) is session-scoped and not committed; its design is recorded
here so the JS-math and data-island checks can be regenerated if needed.

## Next steps

1. **Widget activations** (the deferred workstream): ~13 static-widget activations + 4 new ch01/ch03
   widgets, using the planning-phase design specs.
2. **`.qmd` corrections** (Q1–Q19): apply in `notebooks_quarto/` (triggers the ipynb/md/book sync).
   Q15 (ch14 gender gap) is the highest-value statistical fix.
3. Optionally regenerate PDFs / rebuild the Quarto book after the `.qmd` corrections.
